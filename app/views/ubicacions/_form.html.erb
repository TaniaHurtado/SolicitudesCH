<%= form_for(ubicacion) do |f| %>
  <% if ubicacion.errors.any? %>
    <div id="error_explanation" class="custom-alert-notice">
      <h2><%= pluralize(ubicacion.errors.count, "error") %> han evitado guardar la ubicación:</h2>
      <ul>
      <% ubicacion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :lugar %>
    <%= f.text_field :lugar %>
  </div>

  <div class="field">
    <%= f.label :responsables %>
          
  </div>
    <%= f.collection_check_boxes :user_ids, @users.all, :id, :nombre do |b|%>
      <%= b.check_box %>
      <%= b.label %>
    <% end %>
  <div class="actions">
      <%= f.submit %>
    
  </div>
<% end %>
